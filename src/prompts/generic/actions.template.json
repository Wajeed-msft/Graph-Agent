[
    {
        "name": "get{WORKLOAD_NAME}s",
        "description": "Retrieves all {WORKLOAD_NAME_LOWER} items that the user has access to",
        "parameters": {
            "type": "object",
            "properties": {
                "filter": {
                    "type": "string",
                    "description": "Optional filter to apply to the results"
                },
                "top": {
                    "type": "number",
                    "description": "Maximum number of items to return"
                }
            },
            "required": []
        }
    },
    {
        "name": "create{WORKLOAD_NAME}",
        "description": "Creates a new {WORKLOAD_NAME_LOWER} item with the specified details",
        "parameters": {
            "type": "object",
            "properties": {
                "{WORKLOAD_PRIMARY_FIELD}": {
                    "type": "string",
                    "description": "The {WORKLOAD_PRIMARY_FIELD_DESCRIPTION}"
                }
                // TODO: Add workload-specific required fields
                // Example for Calendar:
                // "subject": { "type": "string", "description": "The title/subject of the event" },
                // "start": { "type": "string", "description": "Start date and time in ISO format" },
                // "end": { "type": "string", "description": "End date and time in ISO format" }

                // Example for Teams:
                // "displayName": { "type": "string", "description": "The name of the team" },
                // "description": { "type": "string", "description": "Description of the team" }

                // Example for SharePoint:
                // "name": { "type": "string", "description": "The name of the file or folder" },
                // "content": { "type": "string", "description": "Content of the file (for text files)" }
            },
            "required": ["{WORKLOAD_PRIMARY_FIELD}"]
        }
    },
    {
        "name": "get{WORKLOAD_NAME}Details",
        "description": "Retrieves detailed information about a specific {WORKLOAD_NAME_LOWER} item",
        "parameters": {
            "type": "object",
            "properties": {
                "id": {
                    "type": "string",
                    "description": "The ID of the {WORKLOAD_NAME_LOWER} item to get details for"
                }
            },
            "required": ["id"]
        }
    },
    {
        "name": "update{WORKLOAD_NAME}",
        "description": "Updates an existing {WORKLOAD_NAME_LOWER} item with new information",
        "parameters": {
            "type": "object",
            "properties": {
                "id": {
                    "type": "string",
                    "description": "The ID of the {WORKLOAD_NAME_LOWER} item to update"
                },
                "{WORKLOAD_PRIMARY_FIELD}": {
                    "type": "string",
                    "description": "Updated {WORKLOAD_PRIMARY_FIELD_DESCRIPTION}"
                }
                // TODO: Add other updatable fields based on workload
            },
            "required": ["id"]
        }
    },
    {
        "name": "delete{WORKLOAD_NAME}",
        "description": "Deletes a {WORKLOAD_NAME_LOWER} item",
        "parameters": {
            "type": "object",
            "properties": {
                "id": {
                    "type": "string",
                    "description": "The ID of the {WORKLOAD_NAME_LOWER} item to delete"
                }
            },
            "required": ["id"]
        }
    },
    {
        "name": "getMy{WORKLOAD_NAME}s",
        "description": "Retrieves {WORKLOAD_NAME_LOWER} items specifically related to the current user",
        "parameters": {
            "type": "object",
            "properties": {},
            "required": []
        }
    }
    // TODO: Add workload-specific actions
    // Example for Calendar:
    // {
    //     "name": "getUpcomingEvents",
    //     "description": "Gets upcoming calendar events for the user",
    //     "parameters": { "type": "object", "properties": { "days": { "type": "number", "description": "Number of days ahead to look" } }, "required": [] }
    // },
    // {
    //     "name": "createMeeting",
    //     "description": "Creates a meeting with attendees",
    //     "parameters": { "type": "object", "properties": { "subject": {...}, "attendees": {...}, "start": {...}, "end": {...} }, "required": ["subject", "attendees", "start", "end"] }
    // }

    // Example for Teams:
    // {
    //     "name": "getTeamChannels",
    //     "description": "Gets channels for a specific team",
    //     "parameters": { "type": "object", "properties": { "teamId": { "type": "string", "description": "ID of the team" } }, "required": ["teamId"] }
    // },
    // {
    //     "name": "sendMessageToChannel",
    //     "description": "Sends a message to a team channel",
    //     "parameters": { "type": "object", "properties": { "teamId": {...}, "channelId": {...}, "message": {...} }, "required": ["teamId", "channelId", "message"] }
    // }

    // Example for SharePoint:
    // {
    //     "name": "getListItems",
    //     "description": "Gets items from a SharePoint list",
    //     "parameters": { "type": "object", "properties": { "siteId": {...}, "listId": {...} }, "required": ["siteId", "listId"] }
    // },
    // {
    //     "name": "uploadFile",
    //     "description": "Uploads a file to SharePoint",
    //     "parameters": { "type": "object", "properties": { "siteId": {...}, "fileName": {...}, "content": {...} }, "required": ["siteId", "fileName", "content"] }
    // }
]